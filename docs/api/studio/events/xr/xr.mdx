import {EventExample} from '@site/src/components/event-example'

# XR Events

XR events are forwarded from the 8th Wall engine. They are emitted on the camera entity and bubble up to the [world.events.globalId](/docs/api/studio/world/events/#properties).

See the lifecycle events for [World Effects](/docs/api/studio/events/xr/world), [Face Effects](/docs/api/studio/events/xr/face), and [Image Targets](/docs/api/studio/events/xr/image-targets).

## Events

### REALITY_READY

This event is emitted when 8th Wall Web has initialized and at least one frame has been successfully processed. This is the recommended time at which any permissions/loading UI should be hidden.

#### Properties

None.

#### Example

<EventExample
  ns='ecs.events'
  event='REALITY_READY'
  target='world.events.globalId'
  hasPayload={false}
  callbackBody={`console.log('Reality ready')`}
/>
